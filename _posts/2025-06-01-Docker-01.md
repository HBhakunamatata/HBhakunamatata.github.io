---
layout: post
title: "Docker"
description: 
date: 2025-06-01
categories: Docker
---


## 1.1 Why

Application behaves variously in different environments  

Application + Dependencies = Container  

Docker 是一种容器技术，用于解决跨环境迁移的问题

## 1.2 Installation

```shell
# ubuntu24
步骤 1：更新系统并安装必要依赖
sudo apt update
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common gnupg lsb-release

步骤 2：添加 Docker 官方 GPG 密钥
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

步骤 3：添加 Docker 官方仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

步骤 4：安装 Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

步骤 5：验证 Docker 安装
docker --version
sudo docker run hello-world

可选配置：设置国内镜像加速
sudo vim /etc/docker/daemon.json

{
    "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://hub-mirror.c.163.com"
    ]
}
保存后重启 Docker 服务：
sudo systemctl daemon-reload
sudo systemctl restart docker
允许非 root 用户运行 Docker
sudo usermod -aG docker $USER
```

## 1.3 架构

一张图片


## 2. Commands

### 2.1 Hosts

```shell
systemctl status docker
systemctl start docker
systemctl stop docker
systemctl enable docker
```

### 2.2 Images

```shell
docker images
docker search [image_name]
docker pull [image_name]:[version]
docker rmi [image_name]
```

### 2.3 Container

```shell
docker ps -a
// create
docker run -i -t --name=c1 [image_name] /bin/bash
docker run -i -d --name=c2 [image_name] /bin/bash
// start
docker start [container_name]
docker stop [container_name]
docker rm [container_name]

docker inspect [container_name]
```

## 3. Data Volumns

```shell
docker run -v [host_path]:[container_path]
```

## 4. Application Deployment

```shell
docker run -i -d -p 3307:3306 --name=c-mysql
-v $PWD/conf:/etc/mysql/conf.d
-v 
-e MySQL_ROOT_Password=123456
mysql:5.6
```

## 5. dockerfile

### 5.1 docker image 原理

本质： 分层的文件系统

操作系统的文件管理系统分为bootfs和rootfs

docker镜像使用宿主机的bootfs

tomcat镜像 = rootfs + jdk + tomcat

分层复用镜像

### 5.2 镜像制作

将已经修改过的容器重新发布为一个新镜像，并导出为一个文件用于共享
被分享者使用文件恢复镜像

```shell
docker commit [container] [new_image_name]:[version]
docker save -o [file_name] [image_name]:[version]
docker load -i [file_name]
```

### 5.3 dockerfile制作方式

方法：照抄

示例：部署springboot项目